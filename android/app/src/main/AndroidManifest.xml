<!-- android:installLocation="auto" is what makes the app eligible to be moved to adoptable
     storage (a USB drive adopted by an Android TV, an adopted SD card), which relocates the
     private data directory — and therefore downloads — along with the APK. Plezy declares none
     of the components this is unsafe for: no app widget, IME, live wallpaper, device admin,
     accessibility service, sync adapter or account authenticator. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:installLocation="auto">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS"/>


    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK"/>
    <uses-permission android:name="android.permission.REORDER_TASKS"/>

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC"/>

    <!-- Explicitly remove media permissions that media_kit may add -->
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" tools:node="remove" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" tools:node="remove" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" tools:node="remove" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" tools:node="remove" />


    <uses-feature android:name="android.software.leanback" android:required="false" />
    <uses-permission android:name="com.android.providers.tv.permission.WRITE_EPG_DATA"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <uses-feature android:name="android.hardware.touchscreen" android:required="false" />

    <uses-feature android:name="android.hardware.type.automotive" android:required="false" />

    <!-- Immersive playback needs 6DoF, but the same APK ships to phones, TVs and
         cars, so this must never be required or those stores stop listing it. -->
    <uses-feature android:name="android.hardware.vr.headtracking" android:version="1" android:required="false" />

    <!-- Horizon OS only exposes XR_FB_passthrough to apps that declare this,
         which is why the capability probe reported it missing. Never required:
         the same APK ships to phones, TVs and cars. -->
    <uses-feature android:name="com.oculus.feature.PASSTHROUGH" android:required="false" />

    <application
        android:label="Plezy"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:banner="@drawable/tv_banner"
        android:appCategory="video"
        android:largeHeap="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:usesCleartextTraffic="true">
        <!-- Driver-distraction state on Android Automotive OS. Optional: the platform library is
             absent on every other form factor, and CarRestrictionsMonitor falls back silently. -->
        <uses-library android:name="android.car" android:required="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:supportsPictureInPicture="true"
            android:configChanges="orientation|keyboardHidden|keyboard|navigation|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <meta-data
                android:name="flutter_deeplinking_enabled"
                android:value="false" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
                <category android:name="android.intent.category.LEANBACK_LAUNCHER"/>
                <!-- Horizon OS hybrid apps label every activity as panel or
                     immersive. Extra categories never narrow intent matching,
                     so this is inert on every other platform. -->
                <category android:name="com.oculus.intent.category.2D"/>
            </intent-filter>
            <!-- Deep link handler for Watch Next items -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="plezy" android:host="play"/>
            </intent-filter>
        </activity>
        <!-- Phase 2 scaffolding: reports the headset's OpenXR capabilities to
             logcat and exits. Exported only so `adb shell am start` can reach
             it during development; it takes no input, touches no app data and
             renders nothing. Delete this along with XrProbeActivity once the
             immersive player activity supersedes it. -->
        <activity
            android:name=".xr.XrProbeActivity"
            android:exported="true"
            android:excludeFromRecents="true"
            android:noHistory="true"
            android:theme="@android:style/Theme.NoDisplay" />
        <!-- The immersive half of the hybrid app. Horizon OS switches the
             headset out of the 2D panel shell when an activity carrying
             com.oculus.intent.category.VR is started. Exported so adb can
             launch it during development; it will be started by the app
             itself once playback hands over to it. -->
        <activity
            android:name=".xr.ImmersivePlayerActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:excludeFromRecents="false"
            android:configChanges="orientation|keyboardHidden|keyboard|navigation|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:theme="@android:style/Theme.Black.NoTitleBar.Fullscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="com.oculus.intent.category.VR"/>
                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>
        </activity>
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.edde746.plezy.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_provider_paths" />
        </provider>
        <!-- Launcher shelf artwork. Not exported: readers reach it only through the
             per-URI read grants issued to CATEGORY_HOME packages by WatchNextProvider. -->
        <provider
            android:name=".watchnext.SystemShelfArtworkProvider"
            android:authorities="com.edde746.plezy.systemshelf.artwork"
            android:exported="false"
            android:grantUriPermissions="true" />
        <receiver
            android:name=".watchnext.SystemShelfUpdateReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- Required for url_launcher to open HTTPS URLs -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="http" />
        </intent>
        <!-- Required to resolve external video player apps on Android 11+ -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:mimeType="video/*" />
        </intent>
        <!-- Required to identify HOME launchers as shelf artwork consumers. -->
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.HOME" />
        </intent>
    </queries>
</manifest>
